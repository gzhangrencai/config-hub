{
  "name": "@confighub/sdk",
  "version": "1.0.0",
  "description": "Node.js SDK for ConfigHub configuration management platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "echo \"No tests configured\" && exit 0",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "config",
    "configuration",
    "management",
    "confighub"
  ],
  "author": "ConfigHub",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
